---
description: Campos pendientes al implementar registro Pareja Swinger (pareja_grupo)
globs: public/js/data/registro-adultos*.js, scripts/config-registro-adultos-schema.json
alwaysApply: false
---

# Pareja Swinger — campos pendientes

La subcategoría `swinger` (arquetipo `pareja_grupo`) **aún no tiene formulario de registro** en el wizard escort. Cuando se implemente, agregar:

## Campos públicos obligatorios a incluir

| Campo | Tipo | Opciones |
|-------|------|----------|
| `realizaGangBang` | select | Sí · No · Bajo acuerdo previo |
| `colaboracionContenidoEnPareja` | select | Sí · No · Bajo acuerdo previo |

## Notas

- Referencia en schema: `scripts/config-registro-adultos-schema.json` → subcategoría `swinger` → `delta.pendienteRegistro`.
- Patrón ya usado en Singles (`colaboracionContenido`), Hotwife (`realizaGangBang` + `colaboracionContenido`) y Escort (`realizaTrios`, etc.).
- Persistir vía `mapToPerfil`, `registro-perfil-submit.js` y ficha en `perfil-publico.html`.
