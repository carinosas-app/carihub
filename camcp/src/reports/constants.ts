/** Frozen Phase 1 — Reports Engine v2 schema version. */
export const REPORT_SCHEMA_VERSION = '2.0.0' as const;

export const SSOT_SNAPSHOT_POLICY = 'reference-only' as const;

export const INDEX_MAX_ENTRIES = 100;
